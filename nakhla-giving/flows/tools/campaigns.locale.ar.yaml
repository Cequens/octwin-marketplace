flow_id: campaigns

strings:
  intro:  'عندنا {count} حملة نشطة حالياً — اختر اللي يلمس قلبك.'

  # Carousel card copy. A card has no title field of its own, so the name is the
  # first markdown line of the body; `**…**` renders as bold on both channels.
  card_title:    '**{title}**'
  card_progress: '🎯 جمعنا {raised} من {goal}'
  btn_give:      '🌱 تبرّع لهذي'
